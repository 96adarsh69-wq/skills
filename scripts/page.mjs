import {readFile} from 'node:fs/promises';
import {escapeHTML as esc,citation,sourceFor,stamp} from './core.mjs';
import {renderMindMap,icon,COLOURS,WASHES} from './map.mjs';
const spark='<svg viewBox="0 0 32 32" aria-hidden="true"><path d="m16 0 3.5 11.5L31 16l-11.5 3.5L16 32l-3.5-12.5L0 16l12.5-4.5z" fill="currentColor"/></svg>';
function citations(item,segments,source){const byId=new Map(segments.map(s=>[s.id,s]));return [...new Map(item.segment_ids.map(id=>{const c=citation(source,byId.get(id));return [c.label,c];})).values()].map(c=>c.url?`<a class="cite" href="${esc(c.url)}" target="_blank" rel="noopener" title="${esc(c.title)}">${esc(c.label)} ↗</a>`:`<span class="cite">${esc(c.label)}</span>`).join(' ');}
function evidence(item,segments,source){const ids=new Set(item.segment_ids);return segments.filter(s=>ids.has(s.id)).map(s=>`<p><b>${esc(citation(source,s).label)}</b> ${esc(s.text)}</p>`).join('');}
const player=run=>`<section class="player" aria-label="Audio brief"><div class="player-top">${run.audio_ready?'<button class="play-button" type="button" aria-label="Play audio brief"><span class="play-icon">▶</span><span class="pause-icon">Ⅱ</span></button>':''}<div><div class="player-title">${run.audio_ready?'Listen to the brief':'Written brief ready'}</div><div class="player-note">${run.audio_ready?'Single narrator · '+stamp(run.audio_seconds):'Audio has not been generated'}</div></div><div class="audio-bars" aria-hidden="true">${[8,17,23,13,19,10].map((h,i)=>`<span style="--h:${h}px;--d:-${i*.2}s"></span>`).join('')}</div></div>${run.audio_ready?`<audio id="digest-audio" controls preload="metadata" src="digest.mp3"></audio><div class="player-bottom"><span class="elapsed">0:00</span><input class="seek" type="range" min="0" max="1000" value="0" aria-label="Seek through audio brief"><span>${stamp(run.audio_seconds)}</span></div><div class="player-download"><a href="digest.mp3" download>Download audio ↓</a><a href="narration.html" target="_blank">Open script ↗</a></div><p class="player-status" role="status"></p>`:''}</section>`;
export async function renderPage(summary,segments,source,run){
 const css=await readFile(new URL('../assets/page.css',import.meta.url),'utf8'),js=await readFile(new URL('../assets/page.js',import.meta.url),'utf8');
 if(!summary.mind_map){summary={...summary,mind_map:{title:summary.title,topics:summary.takeaways.map((t,i)=>({...t,id:'topic-'+i,title:t.title||'Topic '+(i+1),points:[{...t,label:t.title||'Source point'}]}))},takeaways:summary.takeaways.map((t,i)=>({...t,topic_id:'topic-'+i,sentence:t.sentence||t.text}))};}
 const sources=source.sources||[source],topics=summary.mind_map.topics;
 const title=summary.title.split(' '),last=title.splice(Math.max(0,title.length-2));
 const panels=topics.map((t,i)=>`<div class="map-context" id="context-${i}" data-context="${i}" hidden><div class="context-icon" style="color:${COLOURS[i%6]}">${icon(t.icon,42)}</div><div><span class="eyebrow">Topic ${i+1}</span><h3>${esc(t.title)}</h3><p>${esc(t.text)}</p>${citations(t,segments,source)}<div class="related-links">${summary.takeaways.map((p,k)=>p.topic_id===t.id?`<a href="#summary-${k}">${esc(p.title)} ↘</a>`:'').join('')}</div></div></div>`).join('');
 const rows=summary.takeaways.map((t,i)=>{const ti=topics.findIndex(p=>p.id===t.topic_id);return `<article class="summary-row" id="summary-${i}" data-topic="${ti}"><div class="summary-margin"><span class="summary-number">${String(i+1).padStart(2,'0')}</span><span class="summary-symbol" style="--tone:${COLOURS[ti%6]};--wash:${WASHES[ti%6]}" aria-hidden="true">${icon(topics[ti].icon,39)}</span></div><div class="summary-body" style="--tone:${COLOURS[ti%6]}"><div class="eyebrow">${esc(topics[ti].title)}</div><h3>${esc(t.sentence)}</h3><p>${esc(t.text)}</p><div class="summary-actions">${citations(t,segments,source)}<button type="button" class="map-back" data-map-back="${ti}">Find in map ↗</button><details><summary>Source passages</summary><div class="evidence-copy">${evidence(t,segments,source)}</div></details></div></div></article>`;}).join('');
 return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(summary.title)} · Vestra listening brief</title><style>${css}</style></head><body><header class="site-header"><nav class="nav wrap"><a class="brand" href="#top" aria-label="Vestra, back to top">${spark}vestra</a><span class="edition">listening brief</span><div class="nav-links"><a href="#map">Visual map</a><a href="#summary">Summary</a><a href="narration.html" target="_blank">Voice script ↗</a></div><a class="source-button" href="#sources">${sources.length} sources ↗</a></nav></header><main class="wrap" id="top"><section class="hero"><div class="hero-copy"><div class="eyebrow"><span class="dot"></span>${sources.length} inputs / One listening brief</div><h1>${esc(title.join(' '))} <em>${esc(last.join(' '))}</em></h1><p>${esc(summary.overview.text)}</p><div class="overview-cites">${citations(summary.overview,segments,source)}</div></div><div class="hero-aside"><div class="audio-display"><span class="audio-duration">${run.audio_ready?stamp(run.audio_seconds):'READ'}</span><span class="audio-caption">${run.audio_ready?'YOUR LISTENING BRIEF':'AUDIO PENDING'}</span><span class="audio-star" aria-hidden="true">✳</span></div>${player(run)}<div class="quick-index"><a href="#map"><b>${String(topics.length).padStart(2,'0')}</b> topics ↘</a><a href="narration.html" target="_blank">Voice script ↗</a></div></div></section><section class="map-section" id="map"><div class="section-heading"><div><div class="eyebrow">01 / See the connections</div><h2>The <em>map.</em></h2><p>A visual overview of the concepts. Select a branch to explore its context.</p></div></div><div class="map-toolbar"><span class="map-status" role="status">${topics.length} branches · related ideas grouped together</span><div class="map-controls"><button class="tour-button" type="button" data-tour>▶ Follow the map</button><button class="quiet-button" type="button" data-all>Show all</button><button class="quiet-button" type="button" data-replay>Replay drawing</button><a class="quiet-button" href="mind-map.svg" target="_blank">Open map ↗</a><button class="quiet-button" type="button" data-zoom>Zoom +</button></div></div><div class="map-viewport"><div class="map-stage">${renderMindMap(summary)}</div></div>${panels}<p class="map-explainer">Branches group related concepts. The relationship key below the map shows additional connections supported by the sources.</p></section><section class="takeaways-section" id="summary"><div class="section-heading"><div><div class="eyebrow">02 / Read the explanation</div><h2>The <em>summary.</em></h2><p>The arguments, examples and qualifications behind the map.</p></div></div>${rows}</section><section class="sources-section" id="sources"><div class="eyebrow">03 / What went into this</div><h2>Source desk.</h2><p>This is a selective brief of the extracted material. Open a source for the full detail.</p>${sources.map((s,i)=>`<article class="source-row"><b>${esc(s.id?.replace('src','S')||String(i+1))}</b><div><h3>${esc(s.title)}</h3><p>${esc(s.kind||'source')}${s.pages?' · '+s.pages+' pages':''}${s.duration_seconds?' · '+stamp(s.duration_seconds):''} · ${s.segments||segments.length} extracted passages</p>${s.note?`<p class="source-note">${esc(s.note)}</p>`:''}</div>${s.url||s.local_href?`<a href="${esc(s.url||s.local_href)}" target="_blank" rel="noopener">Open ↗</a>`:'<span>Uploaded text</span>'}</article>`).join('')}${(source.failures||[]).map(f=>`<article class="source-row failed"><b>Not included</b><div><h3>${esc(f.title)}</h3><p>${esc(f.reason)}</p></div></article>`).join('')}</section><section class="script-link"><div><span class="eyebrow">The exact words in your audio</span><h2>Read the voice script.</h2></div><a href="narration.html" target="_blank">Open script ↗</a></section><footer class="footer"><span>AI-generated brief · Claims remain attributed to their sources.</span><div class="footer-links"><a href="summary.md" download>Summary ↓</a><a href="narration.txt" download>Script ↓</a><a href="mind-map.svg" download>Map ↓</a></div><button class="motion-toggle" type="button" aria-pressed="true"><span class="motion-label">Pause motion</span></button></footer></main><script>${js}</script></body></html>`;
}
export async function renderNarration(summary,segments,source,run){return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Voice script · ${esc(summary.title)}</title><style>body{margin:0;background:#faf7ee;color:#343b31;font-family:Arial,sans-serif}main{max-width:760px;margin:60px auto;padding:0 24px}nav{display:flex;justify-content:space-between;gap:15px;font-size:13px}a{color:inherit;text-underline-offset:4px}h1{font:400 clamp(32px,6vw,58px)/1.1 Georgia,serif;letter-spacing:-2px}p{font-size:20px;line-height:1.8;margin:32px 0 12px}.meta{font:12px monospace;letter-spacing:1px;text-transform:uppercase;margin-top:40px}.cite{display:inline-block;font-size:11px;margin:0 7px 8px 0;background:#e5ebd9;padding:6px}audio{width:100%;margin:20px 0}footer{margin:50px 0;font-size:12px;color:#5a6255}@media print{nav,audio,.cite{display:none}main{margin:0 auto}p{font-size:13px}}</style></head><body><main><nav><a href="index.html">← Back to visual brief</a><a href="narration.txt" download>Download script ↓</a></nav><div class="meta">Voice script${run.audio_seconds?' / '+stamp(run.audio_seconds):''} / ${summary.narration.map(p=>p.text).join(' ').split(/\s+/).length} words</div><h1>${esc(summary.title)}</h1>${run.audio_ready?'<audio controls preload="metadata" src="digest.mp3"></audio>':''}${summary.narration.map(p=>`<p>${esc(p.text)}</p><div>${citations(p,segments,source)}</div>`).join('')}<footer>This is the narration text. Source references shown below paragraphs are not spoken.</footer></main></body></html>`;}
